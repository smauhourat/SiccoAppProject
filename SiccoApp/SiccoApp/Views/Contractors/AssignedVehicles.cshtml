@model SiccoApp.Models.AssignedVehiclesViewModel

@{
    ViewBag.Title = @Resources.AssignedVehicles;
    Layout = "~/Views/Shared/_LayoutCustomer.cshtml";
}
<div class="right_col" role="main">
    <div class="">
        <div class="page-title">
            <div class="title_left">
                <h3>@Resources.AssignedVehicles</h3>
            </div>
            <div class="clearfix"></div>
            <div class="row">
                <div class="col-md-12 col-sm-12 col-xs-12">
                    <div class="x_panel">
                        <div class="x_title">
                            <h2>@Resources.EtyContract : @Html.DisplayFor(model => model.ContractDescription)<small></small></h2>
                            <ul class="nav navbar-right panel_toolbox">
                                <li>
                                    <button type="button" class="btn btn-success" onclick="location.href='@Url.Action("VehiclesToAssign", "Contractors", new {contractID = Model.ContractID, contractorID = Model.ContractorID})'">@Resources.ToAssignVehicle</button>
                                </li>
                            </ul>
                            <div class="clearfix"></div>
                        </div>

                        <div class="x_content">

                            <!--<p>Add class <code>bulk_action</code> to table for bulk actions options on row select</p>-->


                            <table class="table table-striped responsive-utilities jambo_table bulk_action">
                                <thead>
                                    <tr class="headings">
                                        <th class="column-title">@Resources.VehicleIdentificationNumber </th>
                                        <th class="column-title">@Resources.VehicleDescription </th>
                                        <th class="column-title no-link text-right">
                                            <span class="nobr">Action</span>
                                        </th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var item in Model.VehiclesContracts)
                                    {
                                        <tr class="odd pointer">
                                            <td class=" ">@Html.DisplayFor(modelItem => item.Vehicle.IdentificationNumber) </td>
                                            <td class=" ">@Html.DisplayFor(modelItem => item.Vehicle.Description) </td>
                                            <td class="a-center a-center text-right">
                                                @Html.ActionLink(@Resources.Delete, "VehiclesToUnassign", new { vehicleID = item.VehicleID, contractID = item.ContractID  })
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>

                        <div class="nav navbar-right panel_toolbox">
                            <button type="button" class="btn btn-success" onclick="location.href='@Url.Action("VehiclesToAssign", "Contractors", new {contractID = Model.ContractID, contractorID = Model.ContractorID})'">@Resources.ToAssignVehicle</button>
                        </div>

                    </div>
                    <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-4">
                        <button type="button" class="btn btn-primary" onclick="location.href='@Url.Action("Contracts", "Contractors", new { id = Model.ContractorID })';return false;">@Resources.BackToList</button>
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>