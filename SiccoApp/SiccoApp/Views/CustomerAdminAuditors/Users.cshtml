@model SiccoApp.Models.CustomerAuditorsListViewModel

@{
    ViewBag.Title = "Customer Auditor Users";
    //Layout = "~/Views/Shared/_LayoutCustomer.cshtml";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
<div class="right_col" role="main">
    <div class="">
        <div class="page-title">
            <div class="title_left">
                <h3>@Resources.Users</h3>
            </div>
            <div class="clearfix"></div>
            <div class="row">
                <div class="col-md-12 col-sm-12 col-xs-12">
                    <div class="x_panel">
                        <div class="x_title">
                            <h2>@Resources.EtyAuditors <small><!--detalle subtitulo--></small></h2>
                            <ul class="nav navbar-right panel_toolbox">
                                <li>
                                    <button type="button" class="btn btn-success" onclick="location.href='@Url.Action("UsersCreate", "CustomerAdminAuditors", new { customerID = Model.CustomerID })'">@Resources.CreateNew</button>
                                </li>
                            </ul>
                            <div class="clearfix"></div>
                        </div>

                        <div class="x_content">

                            <!--<p>Add class <code>bulk_action</code> to table for bulk actions options on row select</p>-->

                            <div class="table-responsive">
                                <table class="table table-striped responsive-utilities jambo_table bulk_action">
                                    <thead>
                                        <tr class="headings">
                                            <th class="column-title">@Resources.UserName </th>
                                            <th class="column-title">@Resources.FirstName </th>
                                            <th class="column-title">@Resources.LastName </th>
                                            <th class="column-title">@Resources.Email </th>
                                            <th class="column-title no-link text-right">
                                                <span class="nobr">Acciones</span>
                                            </th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var item in Model.CustomerAuditorUsers)
                                        {
                                            <tr class="odd pointer">
                                                <td class="">@Html.DisplayFor(modelItem => item.UserName) </td>
                                                <td class="">@Html.DisplayFor(modelItem => item.FirstName) </td>
                                                <td class="text-nowrap">@Html.DisplayFor(modelItem => item.LastName) </td>
                                                <td class="">@Html.DisplayFor(modelItem => item.Email) </td>
                                                <td class="a-center a-center text-nowrap text-right">
                                                    @*@Html.ActionLink(@Resources.Edit, "UsersEdit", new { customerAuditorID = item.CustomerAuditorID }) |*@
                                                    @Html.ActionLink(@Resources.Delete, "UsersDelete", new { customerAuditorID = 0, customerID = item.CustomerID, userId = item.UserId }) |
                                                    @Html.ActionLink(@Resources.AssignedCustomers, "AssignedCustomers", new { userID = item.UserId })
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>

                        <div class="nav navbar-right panel_toolbox">
                            <button type="button" class="btn btn-success" onclick="location.href='@Url.Action("UsersCreate", "CustomerAdminAuditors", new { customerID = Model.CustomerID })'">@Resources.CreateNew</button>
                        </div>
                    </div>

                    <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-4">
                        
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
